---
description: Scaffold the current folder as an Ironsworn campaign (safe to re-run — never overwrites existing files)
allowed-tools: ["Bash"]
---

Run the ironsworn-init scaffold script using Bash:

```bash
bash "${CLAUDE_PLUGIN_ROOT}/commands/ironsworn-init.sh"
```

Show the script output verbatim to the user without summarizing it.
