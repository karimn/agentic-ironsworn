# @graphiti/shared (vendored)

Vendored build of `@graphiti/shared` from the `karimn/graphiti-ts` fork
(commit `5f23b5d5b3bd21d6ee864a4ae667ed6d0f35d0a6`). See
`packages/graphiti-core/README.md` for the full rationale and the
update procedure — both packages are vendored together from the same
fork commit.
